generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model PartidaPersistida {
  id        String   @id
  estado    Json
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@map("partidas")
}
